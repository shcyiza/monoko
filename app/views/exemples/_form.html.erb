<%= form_for(@exemple) do |f| %>
  <% if @exemple.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exemple.errors.count, "error") %> prohibited this exemple from being saved:</h2>

      <ul>
      <% @exemple.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">

    <div class="input-field">
      <%= f.text_area :content, label: false, placeholder: "Ajoutez un exemple pour cette définition", class: "materialize-textarea" %>
    </div>
  </div>
  <!--<div class="field">
    <%= f.label :is_fr %><br>
    <%= f.check_box :is_fr %>
  </div>
  <div class="field">
    <%= f.label :is_li %><br>
    <%= f.check_box :is_li %>
  </div>-->
  <div class="field">

    <%= f.hidden_field :word_id, value: word.id %>
  </div>

  <% if user_signed_in? %>
    <%= f.hidden_field :contributor_id, value: current_user.id %>
  <% end %>

  <div class="field">
    <%= f.label :source %><br>
    <%= f.text_field :source %>
  </div>
  <div class="field">
    <%= f.label :is_validated %><br>
    <%= f.check_box :is_validated %>
  </div> -->
  <div class="actions">
    <%= f.submit "Ajouter" , class: "btn right" %>
  </div>
  <br>
<% end %>
