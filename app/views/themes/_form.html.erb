<%= form_for(@theme) do |f| %>
  <% if @theme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@theme.errors.count, "error") %> prohibited this theme from being saved:</h2>

      <ul>
      <% @theme.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
  <div class="col m8 s6">
    <div class="input-field">
      <%= f.text_field :name, label: false, placeholder: "Ajoutez un nouveau champ lexical" %>
    </div>
  </div>

  <div class="col m2 s3">
      <div class="input-field">
        <%= f.check_box :is_fr %>
        <%= f.label :is_fr, "français" %>
      </div>
    </div>

    <div class="col m2 s3">
      <div class="input-field">
        <%= f.check_box :is_li %>
        <%= f.label :is_li, "lingala" %>
      </div>
    </div>
    </div>


  <% if user_signed_in? %>
    <%= f.hidden_field :contributor_id, value: current_user.id %>
<% end %>


<!--
  <div class="field">
    <%= f.label :source %><br>
    <%= f.text_field :source %>
  </div>
  <div class="field">
    <%= f.label :is_validated %><br>
    <%= f.check_box :is_validated %>
  </div>
  -->
  <div class="actions">
    <%= f.submit "Ajouter", class: "btn right" %>
    <br>
  </div>
<% end %>
