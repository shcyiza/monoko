 
<h3 class="center-align">Dictionnaire alphabétique</h3>

 <div class="collection">
    <% @words.each do |word| %>
      <div class="collection-item">
      <h5><%= link_to word.name, word_path(word) %> <small>(<i><%= word.prononciation if word.prononciation %></i>)</small>
        <span class="badge">
          <% if word.is_fr %>français<% end %>
          <% if word.is_li %>lingala<% end %>
        </span>
        <span class="new badge purple" data-badge-caption="définitions"><%= word.definitions.count %></span>
      </h5>
      <p><%= word.definitions.last.content if word.definitions.any? %></p>

       
        <!--<td><%= word.contributor_id %></td>
        <td><%= word.source %></td>
        <td><%= word.is_validated %></td>
        <td><%= link_to 'Show', word %></td>
        <td><%= link_to 'Edit', edit_word_path(word) %></td>
        <td><%= link_to 'Destroy', word, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
      
    </div>
    <% end %>
</div>

<br>

<!--<%= link_to 'New Word', new_word_path %> -->

<a class="btn-floating btn-large waves-effect waves-light right" href="/words/new"><i class="material-icons">edit</i></a>
