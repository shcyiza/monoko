<%= form_for(@search) do |f| %>
  <% if @search.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@search.errors.count, "error") %> prohibited this search from being saved:</h2>

      <ul>
      <% @search.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="field row">
      <%= f.text_field :keywords, label: false, placeholder: "Luka ndimbola na Lingala to libongoli na Français / Rechercher une définition ou une traduction en Lingala", class: "col s11" %>
      <%= button_tag class: 'btn btn-sm', type: :submit do %>
        <i class="material-icons prefix col s1">search</i>
      <% end %>
    </div>
    <!--<small>Luka ndimbola na Lingala to libongoli na Français / Rechercher une définition ou une traduction en Lingala</small>-->
<% end %>
