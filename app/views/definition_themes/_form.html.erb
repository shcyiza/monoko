<%= form_for(@definition_theme) do |f| %>
  <% if @definition_theme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@definition_theme.errors.count, "error") %> prohibited this definition_theme from being saved:</h2>

      <ul>
      <% @definition_theme.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    
    <%= f.hidden_field :definition_id, value: definition.id %>
  </div>
  <div class="field">
    <%= f.hidden_field :theme_id, value: theme.id %>
  </div>
  <div class="actions">
    <%= f.submit theme.name, class: "btn" %>
  </div>
<% end %>
