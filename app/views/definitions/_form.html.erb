<%= form_for(@definition) do |f| %>
  <% if @definition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@definition.errors.count, "error") %> prohibited this definition from being saved:</h2>

      <ul>
      <% @definition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <!--<div class="field">
    <%= f.label :is_fr %><br>
    <%= f.check_box :is_fr %>
  </div>
  <div class="field">
    <%= f.label :is_li %><br>
    <%= f.check_box :is_li %>
  </div> -->

    <%= f.hidden_field :word_id, value: @word.id %>

    <%= f.hidden_field :contributor_id, value: current_user.id %>

<div class="row">
  <div class="col m8 s12">
    <div class="input-field">
      <%= f.text_area :content, label: false, placeholder: "Ajoutez une définition de #{@word.name}", class: "materialize-textarea" %>
    </div>

    
    
  </div>
  <div class="col m4 s12">
    <div class="input-field">
      <%= f.text_field :source, label: false, placeholder: "Quelle est votre source ?" %>
    </div>

    <%= f.submit "Ajouter la définition", class: "btn right" %>
  </div>
</div>

<!--<div class="field">
      <%= f.label :is_validated %><br>
      <%= f.check_box :is_validated %>
    </div>-->

  <div class="actions">
    
  </div>
<% end %>
