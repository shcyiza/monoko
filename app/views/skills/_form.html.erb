<%= form_for(@skill) do |f| %>
  <% if @skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skill.errors.count, "error") %> prohibited this skill from being saved:</h2>

      <ul>
      <% @skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :theme_id %><br>
    <%= f.number_field :theme_id %>
  </div>
  <div class="field">
    <%= f.label :certified %><br>
    <%= f.check_box :certified %>
  </div>
  <div class="field">
    <%= f.label :practiced %><br>
    <%= f.check_box :practiced %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
